---
name: setup-dependencies
description: "Dependency and environment setup for this template (uv, LaTeX, cluster)."
---

# Setup Dependencies Skill

## When to Use

- The user is setting up the environment after forking or cloning.
- You need guidance for dependencies, LaTeX, or cluster setup.

## Instructions

- Use `references/dependencies.md` for concrete steps and commands.
- Confirm whether the user needs LaTeX tooling and/or cluster scripts.
- If new dependencies are required, suggest `uv add <package>` and keep changes minimal.
